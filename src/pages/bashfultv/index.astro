---
import BaseLayoutNoAuthor from "../../layouts/BaseLayoutArchive.astro";
const pageTitle="The Idiot Box";
const allPosts = await Astro.glob('./pub/*/*/*/*.md');
---
<BaseLayoutNoAuthor title={pageTitle}>
  <p>
    <ul>
        {allPosts.map((post) => 
        <li><a href={post.url}>{post.frontmatter.title}</a><br/>
        {post.frontmatter.author}, {post.frontmatter.pubDate}<br/>
        {post.frontmatter.description}</li>)}
    </ul>
  <a href="archive">Archive</a>
  </p>
</BaseLayoutNoAuthor>